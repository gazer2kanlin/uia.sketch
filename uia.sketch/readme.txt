1. Use Eclipse IDE to generate JAXB classes:
   a. Add %JDK%\lib\tools.jar to classpath of JRE System Library.
   b. Add '-encoding utf-8' at 'Additional arguments' when generate JAXB classes.
   c. Output classes in folder \uia\sketch\model\xml
